<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      margin: 0;
      background-color: #f4f4f4;
    }

    form div {
      margin-bottom: 15px;
    } 
    label {
      margin-bottom: 5px;
      display: block;
      font-weight: bold;
    }
    input {
      /* width: 100%; */
      padding: 8px;
      box-sizing: border-box;
      border: 1px solid #ccc;
      border-radius: 4px;
    }
    button {
      padding: 10px;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }
    button:hover {
      background-color: #0056b3;
    }
    .message {
      color: red;
      text-align: center;
    }
    footer {
      text-align: center;
      margin: 0;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>設定空間與時間</h1>
    <form action="/users/profile" method="post">
      <div>
        <label for="spaces">空間 (以逗號分隔):</label>
        <input type="text" id="spaces" name="spaces" required>
      </div>
      <div>
        <label for="timeRange">時間範圍 (例如: 8:00-22:00):</label>
        <input type="text" id="timeRange" name="timeRange" required>
      </div>
      <button type="submit">保存</button>
    </form>
    <% if (message) { %>
      <p class="message"><%= message %></p>
    <% } %>
  </div>
</body>
</html>
